<odoo>
  <data>
    <!-- explicit list view definition -->
    <!--
    <record model="ir.ui.view" id="crm_patch.list">
      <field name="name">crm_patch list</field>
      <field name="model">crm_patch.crm_patch</field>
      <field name="arch" type="xml">
        <tree>
          <field name="name"/>
          <field name="value"/>
          <field name="value2"/>
        </tree>
      </field>
    </record>
    -->

    <!-- actions opening views on models -->
    <!--
    <record model="ir.actions.act_window" id="crm_patch.action_window">
      <field name="name">crm_patch window</field>
      <field name="res_model">crm_patch.crm_patch</field>
      <field name="view_mode">tree,form</field>
    </record>
    -->

    <!-- server action to the one above -->
    <!--
    <record model="ir.actions.server" id="crm_patch.action_server">
      <field name="name">crm_patch server</field>
      <field name="model_id" ref="model_crm_patch_crm_patch"/>
      <field name="state">code</field>
      <field name="code">
        action = {
          "type": "ir.actions.act_window",
          "view_mode": "tree,form",
          "res_model": self._name,
        }
      </field>
    </record>
    -->

    <!-- Top menu item -->
    <!--
    <menuitem name="crm_patch" id="crm_patch.menu_root"/>
    -->
    <!-- menu categories -->
    <!--
    <menuitem name="Menu 1" id="crm_patch.menu_1" parent="crm_patch.menu_root"/>
    <menuitem name="Menu 2" id="crm_patch.menu_2" parent="crm_patch.menu_root"/>
    -->
    <!-- actions -->
    <!--
    <menuitem name="List" id="crm_patch.menu_1_list" parent="crm_patch.menu_1"
              action="crm_patch.action_window"/>
    <menuitem name="Server to list" id="crm_patch" parent="crm_patch.menu_2"
              action="crm_patch.action_server"/>
    -->
  </data>
</odoo>